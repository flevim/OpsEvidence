services:
  backend:
    ports:
      - "${LAN_HOST:-192.168.1.13}:${BACKEND_PORT:-8010}:8080"

  frontend:
    build:
      args:
        VITE_API_URL: http://${LAN_HOST:-192.168.1.13}:${BACKEND_PORT:-8010}
    ports:
      - "${LAN_HOST:-192.168.1.13}:${FRONTEND_PORT:-5180}:8080"
